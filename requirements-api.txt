# travel-social-content API service dependencies
# Install with: pip install -r requirements-api.txt

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# LLM providers (pick at least one)
anthropic>=0.18.0
openai>=1.12.0
# httpx is required by both
httpx>=0.26.0

# Optional: dev / docs
pytest>=7.4.0
ruff>=0.4.0
